require 'spec_helper'
require_relative "../../lib/code/1"

describe '#plus' do
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
  it { expect(plus(1,1)).to eq 2 }
end
